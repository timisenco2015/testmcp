```markdown
# Module: SiteOpportunity

## SiteOpportunity-UC1 — Retrieve Site Opportunities by ID

- Primary Actor(s): System
- Secondary Actor(s): None
- Trigger: Request to retrieve site opportunities by specific ID
- Description: This use case retrieves site opportunities from the database by a specific ID.
- Preconditions: Valid ID is provided.
- Postconditions: List of site opportunities for the given ID is returned.
- Main Success Scenario:
  1. System sets the database procedure to "SiteOpportunitiesSearchID".
  2. System adds the parameter "ID" with the provided ID.
  3. System executes the query and fetches data.
  4. System fills the list of SiteOpportunity objects with the retrieved data.
  5. System returns the list to the requester.
- Extensions: None
- Exceptions: None
- Business Rules: None
- Assumptions: The database connection is operational.
- Open Issues: None

## SiteOpportunity-UC2 — Add New Site Opportunity

- Primary Actor(s): User
- Secondary Actor(s): System
- Trigger: User requests to add a new site opportunity.
- Description: This use case allows adding a new site opportunity to the database.
- Preconditions: User has provided all necessary details.
- Postconditions: New site opportunity is added to the database.
- Main Success Scenario:
  1. System validates the provided data.
  2. If valid, system sets the database procedure to "SiteOpportunitiesAdd".
  3. System adds parameters such as "SiteID", "HeadingID", "Response", and "SortOrder".
  4. System executes the query to add the data.
  5. The site opportunity is successfully added to the database.
- Extensions: None
- Exceptions:
  - E1: Invalid data provided.
    - System logs an error message and stops the process.
- Business Rules: None
- Assumptions: User inputs are correct and valid.
- Open Issues: None

## SiteOpportunity-UC3 — Update Site Opportunity

- Primary Actor(s): User
- Secondary Actor(s): System
- Trigger: User requests to update an existing site opportunity.
- Description: This use case allows updating an existing site opportunity in the database.
- Preconditions: Valid ID and updates are provided.
- Postconditions: The specified site opportunity is updated in the database.
- Main Success Scenario:
  1. System checks if the provided ID and data are valid.
  2. System sets the database procedure to "SiteOpportunitiesUpdate".
  3. System updates the site opportunity's parameters.
  4. System executes the update query.
  5. Changes are successfully saved to the database.
- Extensions: None
- Exceptions:
  - E1: Invalid data or ID.
    - System logs an error message and does not perform the update.
- Business Rules: None
- Assumptions: Valid updates are provided by the user.
- Open Issues: None

## SiteOpportunity-UC4 — Delete Site Opportunity

- Primary Actor(s): User
- Secondary Actor(s): System
- Trigger: User requests to delete a site opportunity.
- Description: This use case allows deletion of a site opportunity from the database.
- Preconditions: A valid ID is provided.
- Postconditions: The specified site opportunity is removed from the database.
- Main Success Scenario:
  1. System verifies that the ID is non-zero.
  2. System sets the database procedure to "SiteOpportunitiesDelete".
  3. System adds the parameter "SiteOpportunitiesID".
  4. System executes the query to delete the record.
  5. Site opportunity is successfully deleted from the database.
- Extensions: None
- Exceptions:
  - E1: Invalid or zero ID.
    - System stops the process without deleting.
- Business Rules: None
- Assumptions: ID provided is correct.
- Open Issues: None
```