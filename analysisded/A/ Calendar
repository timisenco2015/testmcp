```json
{
  "filename": "Calendar",
  "found": true,
  "summary": "Source code for disabling a calendar popup on view-only screens.",
  "purpose": "The code allows disabling of a calendar popup by hiding the icon and disabling the calendar extender.",
  "entities": ["WebControl", "Control", "AjaxControlToolkit.CalendarExtender"],
  "fields": ["iconName", "extenderName"],
  "actors": [],
  "workflows": [
    {
      "name": "DisableCalendar",
      "steps": [
        "Find the control with the name 'iconName' within the container.",
        "Set the visibility of the control to false.",
        "Find the calendar extender within the container using 'extenderName'.",
        "Disable the calendar extender if it is found."
      ]
    }
  ],
  "business_rules": [
    "If the icon control is found, it must be hidden.",
    "If the calendar extender is found, it must be disabled."
  ],
  "validations": [],
  "calculations": [],
  "conditions": [
    "Check if the icon control exists before hiding it.",
    "Check if the calendar extender exists before disabling it."
  ],
  "system_behavior": [],
  "dependencies": [],
  "exceptions": [
    "The function assumes that the controls with names 'iconName' and 'extenderName' exist within the 'container'."
  ],
  "source_document_ids": ["004691e2-ee17-54dc-b16c-be71b656c46a"],
  "content_gaps": []
}
```