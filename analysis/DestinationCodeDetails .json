I am ready to retrieve and analyze files as instructed. However, I need a specific filename to begin each retrieval and analysis process.  
Your prompt requests "all files," but the database schema only operates per filename (such as DestinationCodeDetails). I cannot enumerate or retrieve every file at once without a list of filenames.

Please provide one or more filenames you want analyzed, or specify if you want me to enumerate all available filenames from grouped_related_files.  
After each analysis, I will prepare the structured JSON artifact for each file you specify.

Let me know how you would like to proceed:  
- Provide filenames (e.g. DestinationCodeDetails, BookingRules, etc)  
- Or instruct me to enumerate filenames from grouped_related_files and process each sequentially.

TASK:

1. Read the full structured JSON content from I am ready to retrieve and analyze files as instructed. However, I need a specific filename to begin each retrieval and analysis process.  
Your prompt requests "all files," but the database schema only operates per filename (such as DestinationCodeDetails). I cannot enumerate or retrieve every file at once without a list of filenames.

Please provide one or more filenames you want analyzed, or specify if you want me to enumerate all available filenames from grouped_related_files.  
After each analysis, I will prepare the structured JSON artifact for each file you specify.

Let me know how you would like to proceed:  
- Provide filenames (e.g. DestinationCodeDetails, BookingRules, etc)  
- Or instruct me to enumerate filenames from grouped_related_files and process each sequentially. .

2. Use that JSON as the exact file content.

3. Save the file in the repository:

https://github.com/timisenco2015/testmcp.git

4. Use branch:

main

5. File path:

analysis/DestinationCodeDetails .json

6. Commit message:

Add StudentPayHistory analysis

GITHUB WRITE PARAMETERS:

- owner: timisenco2015

- repo: testmcp

- branch: main

- path: analysis/DestinationCodeDetails .json

- content: exact JSON from I am ready to retrieve and analyze files as instructed. However, I need a specific filename to begin each retrieval and analysis process.  
Your prompt requests "all files," but the database schema only operates per filename (such as DestinationCodeDetails). I cannot enumerate or retrieve every file at once without a list of filenames.

Please provide one or more filenames you want analyzed, or specify if you want me to enumerate all available filenames from grouped_related_files.  
After each analysis, I will prepare the structured JSON artifact for each file you specify.

Let me know how you would like to proceed:  
- Provide filenames (e.g. DestinationCodeDetails, BookingRules, etc)  
- Or instruct me to enumerate filenames from grouped_related_files and process each sequentially.

- message: Add StudentPayHistory analysis

STRICT RULES:

- Use the content from I am ready to retrieve and analyze files as instructed. However, I need a specific filename to begin each retrieval and analysis process.  
Your prompt requests "all files," but the database schema only operates per filename (such as DestinationCodeDetails). I cannot enumerate or retrieve every file at once without a list of filenames.

Please provide one or more filenames you want analyzed, or specify if you want me to enumerate all available filenames from grouped_related_files.  
After each analysis, I will prepare the structured JSON artifact for each file you specify.

Let me know how you would like to proceed:  
- Provide filenames (e.g. DestinationCodeDetails, BookingRules, etc)  
- Or instruct me to enumerate filenames from grouped_related_files and process each sequentially. exactly as the file body.

- Do NOT modify, summarize, or reinterpret the JSON.

- If I am ready to retrieve and analyze files as instructed. However, I need a specific filename to begin each retrieval and analysis process.  
Your prompt requests "all files," but the database schema only operates per filename (such as DestinationCodeDetails). I cannot enumerate or retrieve every file at once without a list of filenames.

Please provide one or more filenames you want analyzed, or specify if you want me to enumerate all available filenames from grouped_related_files.  
After each analysis, I will prepare the structured JSON artifact for each file you specify.

Let me know how you would like to proceed:  
- Provide filenames (e.g. DestinationCodeDetails, BookingRules, etc)  
- Or instruct me to enumerate filenames from grouped_related_files and process each sequentially. is a string, parse it into valid JSON before writing.

- Ensure the final content written is valid JSON (pretty-printed).

- Do NOT add markdown, comments, or explanations.

- Do NOT wrap content in code blocks.

- Do NOT include any extra text before or after the JSON.

- If input is empty or invalid, do not write the file.

OUTPUT:

Return only a short success or failure message.