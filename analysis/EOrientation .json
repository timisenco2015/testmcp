{"instruction": "Analyze every file in the database, generate a structured JSON for each file, and write each file analysis to Github. Due to the scale (over 200 files), batch the request and process each filename according to schema/rules. Procedure: 1. Retrieve all related documents for each filename. 2. Analyze content per JSON schema. 3. Output valid JSON for each file. 4. Ready to write to Github after generation (let me know your repo details after). Confirm if all files processed at once or a subset (e.g., top 10, 20, custom list).", "procedure": ["Retrieve all related documents for each filename.", "Analyze content per JSON schema.", "Output valid JSON for each file.", "Ready to write to Github after generation (let me know your repo details after)."], "note": "Confirm if all files are processed at once or a subset (e.g., top 10, 20, custom list) to proceed effectively."}