I see you want to generate and analyze all files, then write them to GitHub.  
To clarify and proceed efficiently, I need to:

1. Query all distinct filenames from grouped_related_files.
2. For each filename, retrieve all related documents, perform structured analysis, and output a JSON artifact using the schema provided.
3. Write each artifact as a file to GitHub (please specify the repo, branch, and directory if not default).

Before proceeding:
- Do you want each JSON artifact named exactly as the filename (e.g., ITProvisionRole.json)?
- Please provide GitHub repository information and write permissions (access token or user confirmation).

Confirm or provide details so I can start querying and analyzing each file, then prepare for GitHub commit.