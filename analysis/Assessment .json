To fulfill your request, I need clarification and a few details:

1. **Scope of “Generate all files”**:  
   - Do you mean: retrieve and analyze all files in the database, and generate an analysis JSON for each, following the schema above?
   - Or: you want some kind of file generation, not just retrieval and analysis?

2. **GitHub access**:  
   - I do not have direct access to external services like GitHub from this environment. However, I can generate the JSON files for each document, and give you instructions or code to upload them to GitHub.

3. **Workflow**:
   - I will retrieve the list of all filenames from `grouped_related_files`.
   - For each filename, I will perform the analysis as per your previous instructions.
   - I will generate one JSON file per filename, containing the structured analysis.

**Please confirm the scope**:  
Should I proceed to retrieve all filenames from `grouped_related_files`, analyze each file as described, and give you a downloadable package (e.g., zipped JSON files), and/or instructions for pushing them to GitHub?

Let me know if you want any additional filtering, or if you want this process to be automated for all files in your database.
