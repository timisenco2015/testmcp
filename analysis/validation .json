There are over 200 files in your dataset. For the requested process ("Generate all files and analyse each file and write the file to github"), this involves:

1. Retrieving related document contents for each filename.
2. Analysing each file's retrieved content using the provided schema.
3. Writing each analysed file as a structured JSON to GitHub.

To proceed efficiently (and to avoid overwhelming with too many requests at once), please specify:
- A subset of filenames to generate/analyse (e.g., the first 5, or a particular group).
- The desired GitHub repository/path and authentication method for writing files.

Alternatively, if your intent is to process every file:
- I can batch the retrieval and analysis, then provide the JSON artifacts for your review/upload, or you can grant upload access/config for direct GitHub writes.

How would you like to proceed?
